<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="reserveDao">
    
    <select id="content" parameterType="int" resultType="traveldto">
    select * from travel_trip where travel_num=1
    </select>
    
    <select id="member" parameterType="int" resultType="memberdto">
    select*from member_trip where member_num=1
    </select>
    
    
    
    </mapper>