<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    


<mapper namespace="memberdao">

	<select id="Login" parameterType="String" resultType="memberdto">
		
		select * from Member_trip
			where member_id = #{member_id}
					
	</select>
	
	
	<insert id="Join" parameterType="memberdto">
		insert into member_trip values((select max(member_num) from member_trip) + 1,
 		#{member_id},#{member_name},#{member_pwd, jdbcType=VARCHAR},
 		#{member_phone},#{member_email, jdbcType=VARCHAR},sysdate,#{member_nickname})
	</insert>
	
</mapper>
